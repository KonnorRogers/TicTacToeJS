export default function Player({name, marker} = {}) {
  return {
    name,
    marker,
  };
}

// export default Player;
// export default function Player({name, marker} = {}) {
//   return {
//     name,
//     marker,
//   };
// }
